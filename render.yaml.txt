services:
  - type: web
    name: tv-signal-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -b 0.0.0.0:8080 main_flask_api:app  # استخدم gunicorn
